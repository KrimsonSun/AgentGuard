# 门卫 Agent worker（Python 3.11+）
# 注意：livekit-agents 迭代快，Day1 spike 时以当前版本文档校准插件接线
livekit-agents[openai,silero]~=1.2   # 编排 + VAD/turn detection + OpenAI兼容LLM层(→OpenRouter)
asyncpg~=0.30                        # Neon Postgres（visits/profiles/usage_ledger/app_config）
httpx~=0.28                          # 企业微信 webhook / OpenRouter /generation 对账
python-dotenv~=1.0
fastapi>=0.115                       # admin console（模型运行时切换）
uvicorn[standard]>=0.30
# STT/TTS 插件 Day1 双方向实测后定稿加入，例如：
# livekit-plugins-deepgram / livekit-plugins-cartesia / 国内厂商自写插件
