# AgentGuard 运行时依赖（Python 3.11+）
# 语音 I/O 由 Vapi 托管（Azure STT/TTS），本仓库不含媒体栈。
# LiveKit 自建媒体层已评估后退役，见 docs/HANDOFF.md（P1→P2 演进）。
asyncpg~=0.30          # Neon Postgres（visitors/vehicles/visits/usage_ledger/app_config）
httpx~=0.28            # 企业微信 webhook / OpenRouter 代理与 /generation 对账
python-dotenv~=1.0
fastapi>=0.115         # tools_server（custom-llm 代理 + 工具）+ admin console
uvicorn[standard]>=0.30
openai>=1.50           # OpenRouter(OpenAI 兼容)：门卫查询 Agent / 大脑
